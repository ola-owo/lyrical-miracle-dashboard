[project]
name = "lyric-analyzer-airflow"
version = "0.1.0"
requires-python = ">=3.13, !=3.14" # excluded for now until airflow supports 3.14
dependencies = [
    "lyric-analyzer-base",
    "apache-airflow[celery,google,graphviz]==3.1.6",
    "dlt[duckdb]>=1.20.0",
    "fsspec>=2025.12.0",
]
